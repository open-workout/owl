Not needed yet — see [`../README.md`](../README.md). Add only if a
concrete Python consumer (e.g. tooling/scripting around the conformance
corpus) needs to run `Compile`/`Resolve`/`Progress` outside of Go.
